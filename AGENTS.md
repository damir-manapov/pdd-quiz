# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing
any Expo-specific code (file system, sqlite, sharing, etc.). Alternatively, read the
installed package's own type declarations in `node_modules/<package>/build/**/*.d.ts`.
